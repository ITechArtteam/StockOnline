import * as event from './constants'

var initDistributionGoodsState = {
};

export default (state = initDistributionGoodsState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}


